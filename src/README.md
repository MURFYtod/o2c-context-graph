## Source Folder

This repository's implementation is available under `src/` for submission compatibility.

- `src/app/` contains backend API and static frontend files.
- `src/scripts/` contains helper scripts.

The deploy/runtime entrypoint remains `app.main:app` from the repository root.

